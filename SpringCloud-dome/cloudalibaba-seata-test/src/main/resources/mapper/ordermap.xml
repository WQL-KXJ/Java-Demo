<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cloudalibabaseatatest.dao.orderDao">

    <insert id="insert_order" parameterType="com.example.cloudalibabaseatatest.entity.orderpojo">
        INSERT INTO tb_order (user_id,product_id,total_amount,status) VALUES (#{userId},#{productId},#{totalAmount},#{status})
    </insert>
</mapper>