<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.WQL.mapper.accountmapper">

    <select id="select1" resultType="com.WQL.pojo.jdbccrud">

        select * from  jdbctemplatetest where id=#{id}

    </select>

</mapper>